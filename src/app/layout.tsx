import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Diabify 2.0 - Plataforma de Asesoramiento para Diabetes",
  description: "Plataforma especializada en asesoramiento y atención personalizada para personas con diabetes tipo 1, tipo 2, gestacional, prediabetes y problemas de sobrepeso.",
  keywords: "diabetes, asesoramiento, nutrición, profesionales, recursos, test personalizado",
  authors: [{ name: "Diabify Team" }],
  creator: "Diabify",
  publisher: "Diabify",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}
      >
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
        <Toaster />
      </body>
    </html>
  );
}
